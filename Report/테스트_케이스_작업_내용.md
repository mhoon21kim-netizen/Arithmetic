# 테스트 케이스 작업 내용 보고서

## 문서 정보

**프로젝트명**: 인사관리 앱 시스템 구축 (사칙연산 모듈)  
**테스트 ID**: TC-CMM-001 (Common Module) / TC-AO-001 (Arithmetic Operations)  
**작성자**: 홍길동  
**승인자**: 박문수  
**작성일**: 2020-09-01  
**버전**: v1.0  
**테스트 범위**: 공통 모듈  
**작성일**: 2025-12-16  
**TDD 단계**: RED (테스트 작성 완료)

## 테스트 케이스 개요

### 전체 통계

- **총 테스트 케이스 수**: 10개
- **작성 완료**: 10개 (100%)
- **테스트 파일**: `tests/test_arithmetic.py`
- **테스트 클래스**: `TestArithmetic`
- **테스트 프레임워크**: pytest

### 중요도별 분류

| 중요도 | 테스트 수 | 비율 |
|--------|----------|------|
| 중요 | 6개 | 60% |
| 보통 | 3개 | 30% |
| 낮음 | 1개 | 10% |

### 기능별 분류

| 기능 | 테스트 수 | 비율 |
|------|----------|------|
| 덧셈 | 3개 | 30% |
| 뺄셈 | 1개 | 10% |
| 곱셈 | 2개 | 20% |
| 나눗셈 | 4개 | 40% |

## 테스트 케이스 상세

### 1. 덧셈 테스트

#### TC-001: test_addition_positive_numbers

**테스트 케이스명**: 덧셈 테스트 - 양수  
**중요도**: 중요  
**테스트 목적**: 양수 간의 덧셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `1`
- 두 번째 피연산자: `10`

**예상 결과**: `11`

**테스트 코드**:
```python
def test_addition_positive_numbers(self):
    """덧셈 테스트: 양수 (1 + 10 = 11)"""
    assert Arithmetic.add(1, 10) == 11
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_addition_positive_numbers -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-002: test_addition_with_zero

**테스트 케이스명**: 덧셈 테스트 - 0 포함  
**중요도**: 중요  
**테스트 목적**: 0을 포함한 덧셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `0`
- 두 번째 피연산자: `1`

**예상 결과**: `1`

**테스트 코드**:
```python
def test_addition_with_zero(self):
    """덧셈 테스트: 0 포함 (0 + 1 = 1)"""
    assert Arithmetic.add(0, 1) == 1
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_addition_with_zero -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-003: test_addition_negative_numbers

**테스트 케이스명**: 덧셈 테스트 - 음수  
**중요도**: 보통  
**테스트 목적**: 음수 간의 덧셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `-1`
- 두 번째 피연산자: `-10`

**예상 결과**: `-11`

**테스트 코드**:
```python
def test_addition_negative_numbers(self):
    """덧셈 테스트: 음수 (-1 + (-10) = -11)"""
    assert Arithmetic.add(-1, -10) == -11
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_addition_negative_numbers -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

### 2. 뺄셈 테스트

#### TC-004: test_subtraction

**테스트 케이스명**: 뺄셈 테스트  
**중요도**: 중요  
**테스트 목적**: 기본 뺄셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `5`
- 두 번째 피연산자: `2`

**예상 결과**: `3`

**테스트 코드**:
```python
def test_subtraction(self):
    """뺄셈 테스트 (5 - 2 = 3)"""
    assert Arithmetic.subtract(5, 2) == 3
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_subtraction -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

### 3. 곱셈 테스트

#### TC-005: test_multiplication_negative_numbers

**테스트 케이스명**: 곱셈 테스트 - 음수  
**중요도**: 보통  
**테스트 목적**: 음수 간의 곱셈 연산 정확도 검증 (음수 × 음수 = 양수)

**입력값**:
- 첫 번째 피연산자: `-5`
- 두 번째 피연산자: `-3`

**예상 결과**: `15`

**테스트 코드**:
```python
def test_multiplication_negative_numbers(self):
    """곱셈 테스트: 음수 (-5 * -3 = 15)"""
    assert Arithmetic.multiply(-5, -3) == 15
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_multiplication_negative_numbers -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-006: test_multiplication_with_zero

**테스트 케이스명**: 곱셈 테스트 - 0 포함  
**중요도**: 낮음  
**테스트 목적**: 0을 포함한 곱셈 연산 정확도 검증 (0 × 어떤 수 = 0)

**입력값**:
- 첫 번째 피연산자: `0`
- 두 번째 피연산자: `10`

**예상 결과**: `0`

**테스트 코드**:
```python
def test_multiplication_with_zero(self):
    """곱셈 테스트: 0 포함 (0 * 10 = 0)"""
    assert Arithmetic.multiply(0, 10) == 0
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_multiplication_with_zero -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

### 4. 나눗셈 테스트

#### TC-007: test_division_integer

**테스트 케이스명**: 정수 나눗셈 테스트  
**중요도**: 중요  
**테스트 목적**: 정수 나눗셈 연산 정확도 검증 (몫만 반환)

**입력값**:
- 첫 번째 피연산자: `5`
- 두 번째 피연산자: `2`

**예상 결과**: `2` (정수 나눗셈, 소수점 버림)

**테스트 코드**:
```python
def test_division_integer(self):
    """정수 나눗셈 테스트 (5 / 2 = 2)"""
    assert Arithmetic.divide(5, 2) == 2
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_division_integer -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-008: test_division_quotient

**테스트 케이스명**: 소수점 나눗셈 테스트  
**중요도**: 보통  
**테스트 목적**: 소수점을 포함한 나눗셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `5`
- 두 번째 피연산자: `2`

**예상 결과**: `2.5` (소수점 포함)

**테스트 코드**:
```python
def test_division_quotient(self):
    """소수점 나눗셈 테스트 (5 ÷ 2 = 2.5)"""
    assert Arithmetic.quotient(5, 2) == 2.5
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_division_quotient -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-009: test_division_negative

**테스트 케이스명**: 나눗셈 테스트 - 음수  
**중요도**: 중요  
**테스트 목적**: 음수를 포함한 나눗셈 연산 정확도 검증

**입력값**:
- 첫 번째 피연산자: `-10`
- 두 번째 피연산자: `2`

**예상 결과**: `-5`

**테스트 코드**:
```python
def test_division_negative(self):
    """나눗셈 테스트: 음수 (-10 / 2 = -5)"""
    assert Arithmetic.divide(-10, 2) == -5
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_division_negative -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

---

#### TC-010: test_division_by_zero

**테스트 케이스명**: 예외 처리 테스트 - 0으로 나누기  
**중요도**: 중요  
**테스트 목적**: 0으로 나누기 시 예외 처리 검증

**입력값**:
- 첫 번째 피연산자: `0`
- 두 번째 피연산자: `0`

**예상 결과**: `ArithmeticError` 예외 발생

**테스트 코드**:
```python
def test_division_by_zero(self):
    """예외 처리 테스트: 0으로 나누기 (0 / 0 = ArithmeticException)"""
    with pytest.raises(ArithmeticError):
        Arithmetic.divide(0, 0)
```

**실행 명령어**:
```bash
pytest tests/test_arithmetic.py::TestArithmetic::test_division_by_zero -v
```

**현재 상태**: ❌ 실패 (RED 단계 - 구현 코드 없음)

**특이사항**: 
- 이 테스트는 예외가 발생해야 통과하는 테스트입니다.
- `pytest.raises()` 컨텍스트 매니저를 사용하여 예외 발생을 검증합니다.

---

## 테스트 파일 구조

### 파일 위치
```
tests/
└── test_arithmetic.py
```

### 파일 내용 구조

```python
"""
사칙연산 모듈 테스트 케이스
TC-CMM-001 / TC-AO-001
"""
import pytest
from src.arithmetic import Arithmetic


class TestArithmetic:
    """사칙연산 테스트 클래스"""
    
    # 덧셈 테스트 (3개)
    def test_addition_positive_numbers(self): ...
    def test_addition_with_zero(self): ...
    def test_addition_negative_numbers(self): ...
    
    # 뺄셈 테스트 (1개)
    def test_subtraction(self): ...
    
    # 곱셈 테스트 (2개)
    def test_multiplication_negative_numbers(self): ...
    def test_multiplication_with_zero(self): ...
    
    # 나눗셈 테스트 (4개)
    def test_division_integer(self): ...
    def test_division_quotient(self): ...
    def test_division_negative(self): ...
    def test_division_by_zero(self): ...
```

## 테스트 실행 방법

### 전체 테스트 실행

```bash
# 모든 테스트 실행
pytest tests/test_arithmetic.py

# 상세 출력과 함께 실행
pytest tests/test_arithmetic.py -v

# 더 상세한 출력
pytest tests/test_arithmetic.py -vv
```

### 특정 테스트 실행

```bash
# 특정 테스트 함수 실행
pytest tests/test_arithmetic.py::TestArithmetic::test_addition_positive_numbers -v

# 특정 클래스의 모든 테스트 실행
pytest tests/test_arithmetic.py::TestArithmetic -v
```

### 테스트 실행 결과 확인

```bash
# 테스트 결과 요약
pytest tests/test_arithmetic.py --tb=short

# 테스트 결과 상세 출력
pytest tests/test_arithmetic.py --tb=long
```

## 현재 테스트 상태

### 실행 결과 요약

| 테스트 ID | 테스트 함수명 | 상태 | 원인 |
|----------|--------------|------|------|
| TC-001 | test_addition_positive_numbers | ❌ 실패 | ModuleNotFoundError |
| TC-002 | test_addition_with_zero | ❌ 실패 | ModuleNotFoundError |
| TC-003 | test_addition_negative_numbers | ❌ 실패 | ModuleNotFoundError |
| TC-004 | test_subtraction | ❌ 실패 | ModuleNotFoundError |
| TC-005 | test_multiplication_negative_numbers | ❌ 실패 | ModuleNotFoundError |
| TC-006 | test_multiplication_with_zero | ❌ 실패 | ModuleNotFoundError |
| TC-007 | test_division_integer | ❌ 실패 | ModuleNotFoundError |
| TC-008 | test_division_quotient | ❌ 실패 | ModuleNotFoundError |
| TC-009 | test_division_negative | ❌ 실패 | ModuleNotFoundError |
| TC-010 | test_division_by_zero | ❌ 실패 | ModuleNotFoundError |

### 공통 에러

**에러 메시지**:
```
ModuleNotFoundError: No module named 'src.arithmetic'
```

**원인**: 
- `src/arithmetic.py` 파일이 존재하지 않음
- `Arithmetic` 클래스가 구현되지 않음

**상태**: 
- TDD의 RED 단계에서 예상된 실패
- 다음 단계(GREEN)에서 구현 코드 작성 예정

## 테스트 커버리지

### 테스트 케이스 커버리지

- **총 요구사항**: 10개
- **작성 완료**: 10개
- **커버리지**: **100%** ✅

### 중요도별 커버리지

| 중요도 | 요구사항 | 작성 완료 | 커버리지 |
|--------|----------|----------|----------|
| 중요 | 6개 | 6개 | 100% ✅ |
| 보통 | 3개 | 3개 | 100% ✅ |
| 낮음 | 1개 | 1개 | 100% ✅ |

### 기능별 커버리지

| 기능 | 요구사항 | 작성 완료 | 커버리지 |
|------|----------|----------|----------|
| 덧셈 | 3개 | 3개 | 100% ✅ |
| 뺄셈 | 1개 | 1개 | 100% ✅ |
| 곱셈 | 2개 | 2개 | 100% ✅ |
| 나눗셈 | 4개 | 4개 | 100% ✅ |

## 테스트 케이스 매핑

### 요구사항 문서와의 매핑

| 요구사항 문서 | 테스트 ID | 테스트 함수명 | 상태 |
|--------------|----------|--------------|------|
| 덧셈 (양수) | TC-001 | test_addition_positive_numbers | ✅ 작성 완료 |
| 덧셈 (0 포함) | TC-002 | test_addition_with_zero | ✅ 작성 완료 |
| 덧셈 (음수) | TC-003 | test_addition_negative_numbers | ✅ 작성 완료 |
| 뺄셈 | TC-004 | test_subtraction | ✅ 작성 완료 |
| 곱셈 (음수) | TC-005 | test_multiplication_negative_numbers | ✅ 작성 완료 |
| 곱셈 (0 포함) | TC-006 | test_multiplication_with_zero | ✅ 작성 완료 |
| 정수 나눗셈 | TC-007 | test_division_integer | ✅ 작성 완료 |
| 소수점 나눗셈 | TC-008 | test_division_quotient | ✅ 작성 완료 |
| 나눗셈 (음수) | TC-009 | test_division_negative | ✅ 작성 완료 |
| 0으로 나누기 | TC-010 | test_division_by_zero | ✅ 작성 완료 |

## 예상 구현 코드 구조

테스트 케이스를 기반으로 예상되는 구현 코드 구조:

```python
# src/arithmetic.py (예정)

class Arithmetic:
    """사칙연산 클래스"""
    
    @staticmethod
    def add(a, b):
        """덧셈 연산"""
        # TC-001, TC-002, TC-003에서 사용
        pass
    
    @staticmethod
    def subtract(a, b):
        """뺄셈 연산"""
        # TC-004에서 사용
        pass
    
    @staticmethod
    def multiply(a, b):
        """곱셈 연산"""
        # TC-005, TC-006에서 사용
        pass
    
    @staticmethod
    def divide(a, b):
        """정수 나눗셈 연산"""
        # TC-007, TC-009, TC-010에서 사용
        # TC-010: 0으로 나누기 시 ArithmeticError 발생
        pass
    
    @staticmethod
    def quotient(a, b):
        """소수점 나눗셈 연산"""
        # TC-008에서 사용
        pass
```

## 다음 단계

### GREEN 단계 작업 계획

1. **구현 파일 생성**
   - `src/arithmetic.py` 파일 생성
   - `Arithmetic` 클래스 구현

2. **메서드 구현 순서**
   - 우선순위 1: `add()` 메서드 (TC-001, TC-002, TC-003)
   - 우선순위 2: `subtract()` 메서드 (TC-004)
   - 우선순위 3: `multiply()` 메서드 (TC-005, TC-006)
   - 우선순위 4: `divide()` 메서드 (TC-007, TC-009, TC-010)
   - 우선순위 5: `quotient()` 메서드 (TC-008)

3. **테스트 통과 확인**
   - 각 메서드 구현 후 해당 테스트 실행
   - 모든 테스트 통과 확인

## 참고 사항

### 테스트 작성 원칙

1. **명확한 테스트 이름**: 각 테스트 함수명이 테스트 목적을 명확히 표현
2. **독립적인 테스트**: 각 테스트는 독립적으로 실행 가능
3. **단일 책임**: 각 테스트는 하나의 기능만 검증
4. **예외 처리**: 예외 상황도 테스트 케이스로 포함

### TDD 사이클

1. **RED**: 테스트 작성 및 실패 확인 ✅ (완료)
2. **GREEN**: 최소한의 코드로 테스트 통과 ⏳ (다음 단계)
3. **REFACTOR**: 코드 개선 및 최적화 ⏳ (예정)

---

**보고서 작성일**: 2025-12-16  
**작성자**: AI Assistant  
**상태**: RED 단계 완료, 테스트 케이스 100% 작성 완료

